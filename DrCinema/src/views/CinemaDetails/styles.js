import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    marginTop: 15,
    marginBottom: 15,
    textAlign: 'center',
    fontSize: 24,
    fontWeight: 'bold',
  },
});
