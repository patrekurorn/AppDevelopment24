import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    list: {
        flexDirection: 'row',
        justifyContent: 'center',
        paddingBottom: 1,
        paddingLeft: 20
    },
});
